<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.onlinereader">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application


        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Reader_Find">
        </activity>

        <activity android:name=".Reader_Import">
        </activity>
        <activity
            android:name=".Book_Read"
            >
        </activity>

        <activity
            android:name="com.hw.txtreaderlib.ui.HwTxtPlayActivity"
            android:theme="@style/HwTxtFullScreen"></activity>

        <activity
            android:name=".Import_View"
            tools:ignore="DuplicateActivity">
        </activity>
        <activity
            android:name=".RvOneActivity">
        </activity>

    </application>

</manifest>